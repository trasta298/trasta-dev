---
import { css } from '../../styled-system/css'
---

<header
  class={css({
    w: '100vw',
    h: '64px',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    bg: 'bgSecond',
  })}
>
  <div
    class={css({
      display: 'flex',
      alignItems: 'center',
      w: '1024px',
    })}
  >
    <div
      class={css({
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        fontSize: '32px',
        fontWeight: 'bold',
      })}
    >
      <a href="/">Content</a>
    </div>
  </div>
</header>
